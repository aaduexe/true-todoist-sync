{
	"name": "true-todoist-sync",
	"version": "1.0.0",
	"description": "Push tasks from obsidian to todoist, then enjoy two way sync on it.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"todoist",
		"sync",
		"two way sync",
		"task"
	],
	"author": "Aadu",
	"license": "GPL-3.0",
	"devDependencies": {
		"@types/node": "^16.11.6",
		"esbuild": "^0.25.0",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
